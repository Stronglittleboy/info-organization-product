# 复制为 .env 后按需修改（在 code/docker 目录）

# ---------- 本地 Maven 仓库（构建 backend 时 bind 到容器 /root/.m2/repository）----------
# 默认使用本目录 ./maven-cache（持久化在项目内）；与本机 IDE 共用可改为：
# MAVEN_HOST_REPO=/home/你的用户/.m2/repository

# ---------- 代理（compose 已写死 127.0.0.1:20171，可按环境改 compose 或在此处覆盖需扩展变量）----------
# NO_PROXY 传给 backend 构建，避免走代理访问内网服务名
# NO_PROXY=localhost,127.0.0.1,postgres,.fnnas.com

# ---------- Dockerfile 前端镜像（syntax=docker/dockerfile:1.4）----------
# 若私有镜像源对 docker/dockerfile 返回 401，任选其一：
# 1) 本机先执行：docker pull docker.io/docker/dockerfile:1.4
# 2) 在 Docker daemon 为 docker.io/docker/dockerfile 配置可匿名拉取的镜像加速
# 3) 临时关闭镜像源仅拉取该镜像后再打开
