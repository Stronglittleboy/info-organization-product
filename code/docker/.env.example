# 可选：构建 backend 时挂载本机 Maven 仓库，与 Docker 共用依赖缓存（需 BuildKit + Compose additional_contexts）
# 示例（在 code/docker 目录执行 compose 前 export，或复制为 .env）：
# MAVEN_HOST_REPO=/home/你的用户/.m2/repository
#
# 未设置时默认使用本目录下的 ./maven-cache（首次构建会下载依赖并持久化在仓库内）
