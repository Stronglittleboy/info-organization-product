# 若镜像源对 docker/dockerfile 返回 401，请勿在 Dockerfile 首行使用 # syntax=docker/dockerfile:*，
# 也不要使用 RUN --mount（会拉取 Dockerfile 前端）。本地加速依赖可：
#   1) 在本机 code/backend 执行 mvn -Dmaven.test.skip=true package，再把 target/*.jar 用多阶段 COPY；
#   2) 或为 docker.io/docker/dockerfile 配置可匿名拉取的镜像加速。
